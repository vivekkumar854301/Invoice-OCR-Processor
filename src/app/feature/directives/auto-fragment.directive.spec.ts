import { AutoFragmentDirective } from './auto-fragment.directive';

describe('AutoFragmentDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoFragmentDirective();
    expect(directive).toBeTruthy();
  });
});
